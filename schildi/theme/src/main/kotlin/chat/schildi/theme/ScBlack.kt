package chat.schildi.theme

internal val scbMaterialColorScheme = scdMaterialColorScheme.copy(
    primaryContainer = scd_bgBlack,
    background = scd_bgBlack,
    surface = scd_bgBlack,
    surfaceContainer = scd_bgBlack,
)

internal val scbExposures = scdExposures.copy(
    appBarBg = scd_bgBlack,
    spaceBarBg = scd_bgBlack,
)

internal val scbSemanticColors = scdSemanticColors.copy(
    bgCanvasDefault = scd_bgBlack,
)
